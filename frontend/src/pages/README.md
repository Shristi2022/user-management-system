# Pages / Views

Place your core application page views here.
For example, `Dashboard.tsx`, `Login.tsx`, `CreateRecord.tsx`, etc.
These represent full-page views that coordinate multiple UI primitives.

# UI Components (Shadcn UI)

This directory is reserved for Shadcn UI components.
When you run:
`npx shadcn@latest add button` (or card, dialog, input, form, etc.), the CLI will install them directly here.
